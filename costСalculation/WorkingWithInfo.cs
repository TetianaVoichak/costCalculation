using System;
using System.Collections.Generic;
using System.Text;

namespace costСalculation
{
    class WorkingWithInfo
    {
       // List<InfoForDay> infoForDay = new List<InfoForDay>();
        /*void LoadInfoForDay()
        {
            INFOFORDAY = Data.GetInfoForDay();
            //comboBoxList.Items.Clear();
            foreach (var a in INFOFORDAY)
                comboBoxList.Items.Add(a.NameCategory);
            comboBoxList.SelectedIndex = 0;
        }*/
    }
}
